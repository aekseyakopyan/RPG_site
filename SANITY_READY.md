# ✅ SANITY CMS УСПЕШНО УСТАНОВЛЕН!

## 🎉 Что готово

### 1. Sanity Studio запущен
- **URL:** http://localhost:3333
- **Project ID:** 21das8f4
- **Dataset:** production

### 2. Схемы данных созданы
✅ Hero Section  
✅ Services (Услуги)  
✅ Cases (Кейсы)  
✅ Reviews (Отзывы)  
✅ Settings (Настройки)  

### 3. Подключение к сайту настроено
✅ Файл `app/src/lib/sanity.ts` создан  
✅ Project ID добавлен  
✅ Зависимости установлены  

---

## 🚀 КАК НАЧАТЬ РАБОТУ

### Шаг 1: Откройте Sanity Studio

Откройте в браузере: **http://localhost:3333**

Вы увидите админ-панель с 5 типами документов:
- **Hero** — главная секция
- **Services** — услуги
- **Cases** — кейсы
- **Reviews** — отзывы
- **Settings** — настройки сайта

### Шаг 2: Создайте первый документ

Например, создадим услугу:

1. Нажмите на **"Services"** в левом меню
2. Нажмите **"Create"** (зелёная кнопка)
3. Заполните поля:
   - **Название:** PERFORMANCE-МАРКЕТИНГ
   - **Тип:** Основная услуга
   - **Описание:** Полный цикл: от аудита до масштабирования
   - **Что входит:** (добавьте несколько пунктов)
   - **Результат:** Поток лидов по целевому CPA
   - **Срок:** 2 недели на запуск
   - **Цена:** от 80K₽/мес
   - **Иконка:** TrendingUp
   - **Цвет:** from-rpg-gold to-orange-400
   - **Порядок:** 1
4. Нажмите **"Publish"**

### Шаг 3: Подключите к сайту

Откройте любую страницу (например, `ServicesPage.tsx`) и добавьте:

```typescript
import { useEffect, useState } from 'react'
import { getServices } from '@/lib/sanity'

export function ServicesPage() {
  const [services, setServices] = useState([])
  
  useEffect(() => {
    getServices().then(setServices)
  }, [])
  
  return (
    <Layout>
      {services.map(service => (
        <div key={service._id}>
          <h3>{service.name}</h3>
          <p>{service.description}</p>
        </div>
      ))}
    </Layout>
  )
}
```

---

## 📝 ДОСТУПНЫЕ ЗАПРОСЫ

Все запросы уже готовы в `app/src/lib/sanity.ts`:

```typescript
import { 
  getHero,      // Получить Hero секцию
  getServices,  // Получить все услуги
  getCases,     // Получить все кейсы
  getReviews,   // Получить все отзывы
  getSettings,  // Получить настройки сайта
  getCaseById,  // Получить конкретный кейс
  urlFor        // Получить URL изображения
} from '@/lib/sanity'
```

### Пример использования изображений:

```typescript
import { urlFor } from '@/lib/sanity'

<img 
  src={urlFor(service.image).width(800).url()} 
  alt={service.name} 
/>
```

---

## 🎨 ЧТО МОЖНО РЕДАКТИРОВАТЬ

### Hero Section
- Бейдж (PERFORMANCE-МАРКЕТОЛОГ)
- Заголовок с золотым выделением
- Подзаголовок
- Текст кнопок + ссылки
- Изображение героя
- Плавающие бейджи

### Services
- Название, тип, описание
- Список "Что входит"
- Результат, срок, цена
- Иконка (выбор из списка)
- Цвет градиента
- Порядок отображения

### Cases
- Клиент, ниша
- Челлендж
- Решение (список)
- Результаты (до/после)
- Итоговые цифры
- Инструменты, длительность
- Изображение

### Reviews
- Имя, должность, компания
- Текст отзыва
- Рейтинг (1-5 звёзд)
- Фото

### Settings
- Название сайта
- Логотип
- Цвета темы
- Контакты (email, phone, telegram)
- Социальные сети
- SEO метаданные

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### Sanity Studio
```bash
cd sanity-studio

# Запуск dev сервера
npm run dev

# Деплой на sanity.studio
npm run deploy

# Открыть настройки проекта
npx sanity manage
```

### Основной сайт
```bash
cd app

# Запуск dev сервера
npm run dev

# Сборка
npm run build
```

---

## 🌐 ДЕПЛОЙ АДМИНКИ

Когда будете готовы к продакшену:

```bash
cd sanity-studio
npm run deploy
```

Ваша админка будет доступна на:
**https://alexey-marketing.sanity.studio**

Вы сможете редактировать контент откуда угодно!

---

## 📱 МОБИЛЬНОЕ ПРИЛОЖЕНИЕ

Sanity имеет мобильное приложение:

1. Скачайте **"Sanity Studio"** из App Store / Google Play
2. Войдите с теми же учётными данными
3. Редактируйте контент с телефона!

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Sanity Studio запущен
2. ⏳ Создайте первые документы (Hero, Services, Cases)
3. ⏳ Обновите компоненты сайта для получения данных из Sanity
4. ⏳ Протестируйте редактирование
5. ⏳ Мигрируйте все существующие данные
6. ⏳ Задеплойте Sanity Studio

---

## 📚 ДОКУМЕНТАЦИЯ

- [Sanity Docs](https://www.sanity.io/docs)
- [GROQ Query Language](https://www.sanity.io/docs/groq)
- [Image URLs](https://www.sanity.io/docs/image-url)
- [React Integration](https://www.sanity.io/docs/react)

---

## 🆘 ПОМОЩЬ

### Проблемы с подключением?
- Проверьте Project ID в `lib/sanity.ts` (должен быть `21das8f4`)
- Убедитесь, что документы опубликованы (кнопка Publish)

### Изображения не загружаются?
```typescript
// Всегда используйте urlFor
import { urlFor } from '@/lib/sanity'
<img src={urlFor(image).width(800).url()} />
```

### Данные не обновляются?
- Sanity использует CDN, может быть задержка до 1 минуты
- Для мгновенных обновлений: `useCdn: false` в dev режиме

---

## ✅ ГОТОВО!

Теперь у вас есть полноценная админка для управления всем контентом сайта! 🎉

**Сайт:** http://localhost:5173  
**Админка:** http://localhost:3333  
**Project ID:** 21das8f4

Удачи! 🚀
